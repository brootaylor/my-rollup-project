// maths.js

const add = ( x ) => {
	return x + x;
}

const divide = ( x ) => {
	return x / x;
}

const square = ( x ) => {
	return x * x;
}

const cube = ( x ) => {
	return x * x * x;
}

export {
	add,
	divide,
	square,
	cube
}
